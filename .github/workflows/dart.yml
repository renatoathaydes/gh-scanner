name: Dart CI

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install Dart
      run: choco install dart-sdk
    - name: Install dependencies
      run: pub get
    - name: Run tests
      run: pub run test
