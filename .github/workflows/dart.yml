name: Dart CI

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - uses: renatoathaydes/setup-dart@v0.1
    - name: Install dependencies
      run: pub get
    - name: Run tests
      run: pub run test
